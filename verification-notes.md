# 검증 메모

2026-08-27 기준으로 내부 미리보기에서 홈 제목·고정 헤더·CTA·YouTube 플레이어가 정상 렌더링됐다. 홈에는 `학교의 가장 높은 곳,`, `학교의 가장 어두운 곳에서,`, `가장 밝게 빛나다` 세 화면이 노출됐다.

배포 도메인 `pulch23ki-emi48apg.manus.space` 직접 접속은 Manus 인증 게이트로 전환되어 공개 콘텐츠를 브라우저에서 직접 확인할 수 없었다. 이는 공개 사이트 코드 오류가 아니라 배포 인증 상태의 제한으로 기록한다.

## 제목 한 줄·간격 재검증

320px 폭에서 홈·활동기록소·영상·모집 제목은 한 줄을 유지했다. 홈 첫 화면은 제목 아래 설명까지 충분한 간격을 확보했고, 설명 본문과 CTA만 콘텐츠 폭에 맞춰 자연스럽게 줄바꿈됐다. 활동기록소 대표 제목과 모집 안내 제목은 화면을 넘기지 않도록 모바일 전용 글자 크기를 낮췄다.

실제 변경 후 `pnpm check`, `pnpm test`, `pnpm build`를 모두 통과했다. Vitest는 3개 파일, 6개 테스트를 통과했다.

## 모집 콘텐츠 오류 재검증

수정 후 내부 미리보기의 `/recruiting`을 직접 열었다. 23기·2027 기본 모집 제목, 공지, 지원 문의 버튼, 연락처가 정상 렌더링됐고 `content.get(recruiting)` 관련 오류 문구는 페이지에 표시되지 않았다. 브라우저 콘솔도 `No console output`으로 확인됐다.

## 첫 화면 문구 중복 검토

첫 화면 제목 `학교의 가장 높은 곳,`은 관측 장소와 동아리의 상징적 위치를 제시한다. 설명 `망원경을 세우고 오늘의 대상을 고릅니다. 보이는 것을 기록하고, 다음 관측을 준비합니다.`는 관측 절차와 기록 방식을 설명한다. 두 문장은 장소와 활동 과정으로 역할이 분리되어 의미가 직접 중복되지 않으므로 현재 조합을 유지한다.

## 스크롤 길이·관리자 접근 확인

홈 데스크톱 히어로 세 화면의 높이를 `84svh` 중심으로 낮춰 콘텐츠와 순차 스크롤 구조는 유지하면서 전체 세로 길이를 줄였다. 전체 캡처에서 세 화면과 푸터가 연속적으로 표시된다.

내부 미리보기 `/admin`을 직접 열어 브랜드형 로그인 화면, 아이디 입력, 비밀번호 입력, 관리자 로그인 버튼, 공식 사이트 돌아가기 링크가 표시되는 것을 확인했다. 관리자 로그인은 서버 시크릿과 HttpOnly 세션을 사용하는 구조이며, 인증 성공·실패와 세션 복원 테스트가 통과했다.

## 학생 개인정보 보호: 이메일 비노출 검증

모바일 홈과 모집 페이지 전체 캡처에서 푸터와 모집 영역에 이메일 주소가 평문으로 표시되지 않고 `문의하기` 라벨만 표시되는 것을 확인했다. 문의 링크의 `mailto` 대상은 클릭 동작에만 사용되며, 사용자가 누르기 전에는 수신자 주소가 화면 텍스트에 나타나지 않는다. 기존 지원 문의 버튼도 주소 대신 문의 흐름만 표시한다.
