# Pulcherrima 관리자 안내

관리자 화면은 `/admin` 경로에서 접근한다. 최초 총괄관리자의 아이디와 비밀번호는 `PULCHERRIMA_ADMIN_USERNAME`, `PULCHERRIMA_ADMIN_PASSWORD` 서버 시크릿으로 관리하며 클라이언트 번들에 포함하지 않는다. 이 최초 계정으로 로그인하면 데이터베이스에 총괄관리자 계정이 자동으로 연결된다.

로그인 성공 시 서버가 `HttpOnly`, `SameSite=None` 관리자 세션 쿠키를 발급한다. 세션에는 계정 ID와 역할만 서명해 담고, 비밀번호 원문은 절대 담지 않는다. 추가 관리자의 비밀번호는 서버에서 `scrypt` 해시로 저장한다. 브라우저 개발자 도구를 막는 방식은 보안 수단으로 사용하지 않는다.

| 역할 | 가능한 작업 |
|---|---|
| 총괄관리자 | 모든 콘텐츠 편집, 관리자 계정 추가, 아이디·비밀번호·역할 변경, 계정 비활성화 |
| 일반 관리자 | 홈 홍보, 영상 목록, 활동기록소, 모집 안내 콘텐츠 편집 |

총괄관리자는 `/admin`의 **관리자 계정** 탭에서 최대 18명의 활성 계정을 관리할 수 있다. 새 계정은 아이디 3자 이상과 10자 이상의 임시 비밀번호가 필요하다. 마지막 활성 총괄관리자 계정은 일반 관리자로 낮추거나 비활성화할 수 없다. 최초 총괄관리자 시크릿을 바꾸려면 프로젝트의 Secrets 설정에서 두 환경변수를 갱신해야 한다.

활동기록소 탭은 카드 단위 편집 화면이다. 소개 문구, 학기 라벨과 기간을 수정하고, 각 카드의 날짜·종류·제목·설명·이미지 URL·대체 텍스트를 직접 편집한다. `기록 카드 추가`로 새 카드를 만들고, 화살표로 순서를 바꾸거나 휴지통 버튼으로 제거한 뒤 `기록 저장`을 누른다.

홈 홍보·영상 목록·모집 안내 탭은 JSON 형식으로 관리한다. 홈에서는 화면별 문구·설명·영상 ID·이미지 URL·CTA를, 영상 목록에서는 제목·연도·YouTube ID·재생 시간·대표 영상·채널 링크를, 모집 안내에서는 기수·공지·지원 자격·진행 순서·문의 흐름을 수정한다. 콘텐츠는 `siteContents` 테이블의 `home`, `videos`, `archive`, `recruiting` 키로 저장되며, 저장값이 없거나 잘못돼도 공개 페이지는 기본 콘텐츠로 안전하게 렌더링한다.

개인정보 보호를 위해 문의 이메일은 관리자 JSON에 저장하지만 공개 화면에는 주소를 표시하지 않고 `문의하기` 버튼으로만 노출한다. 이미지는 배포 파일에 직접 넣지 말고 지원되는 객체 저장소에 업로드한 URL을 사용한다.
