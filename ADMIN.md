# Pulcherrima 관리자 안내

관리자 화면은 `/admin` 경로에서 접근합니다. 아이디와 비밀번호는 `PULCHERRIMA_ADMIN_USERNAME`, `PULCHERRIMA_ADMIN_PASSWORD` 환경변수로 관리하며 클라이언트 번들에 포함하지 않습니다. 두 값은 프로젝트의 Secrets 설정에서 입력합니다.

로그인 성공 시 서버가 `HttpOnly`, `SameSite=None` 관리자 세션 쿠키를 발급합니다. 관리자 편집 API는 이 세션과 서버 권한 가드를 함께 확인합니다. 브라우저 개발자 도구를 막는 방식은 보안 수단으로 사용하지 않습니다.

관리자 화면에는 네 가지 편집 탭이 있습니다. **홈 홍보**에서는 히어로 문구·설명·화면별 영상 ID·이미지 URL·CTA 라벨과 링크·푸터 안내를 관리합니다. **영상 목록**에서는 제목·연도·YouTube ID·재생 시간·대표 영상 표시·채널 링크를 관리합니다. **활동기록소**에서는 소개 문구·학기명·기간·기록 카드의 번호·날짜·종류·제목·본문·이미지를 관리합니다. **모집 안내**에서는 기수·연도·제목·공지·지원 자격·진행 순서·지원 문의 링크를 관리합니다.

콘텐츠는 서버 데이터베이스의 `siteContents` 테이블에 네 개의 키(`home`, `videos`, `archive`, `recruiting`)로 저장됩니다. 편집기는 JSON 형식을 사용하며 저장 전 각 문서의 핵심 배열과 문자열 필드를 검사합니다. 공개 페이지는 저장된 값이 없거나 잘못된 경우 기본 콘텐츠로 안전하게 렌더링합니다. 사이트의 구조·색상·인증 규칙은 편집 대상에서 제외합니다.

이미지는 배포 파일에 직접 넣지 말고 지원되는 객체 저장소에 업로드한 URL을 사용합니다. YouTube 영상은 영상 ID만 입력하고, 영상 페이지에서 재생 여부를 확인합니다. 공개 개인정보 보호를 위해 문의 이메일은 관리자 JSON에 저장되지만 공개 화면에는 주소가 표시되지 않고 `문의하기` 버튼으로만 노출됩니다.
